{
  "name": "@pipeline/license",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-verifiable Ed25519 license tokens for the open-core Pro/Teams gate. Issue from a Merchant-of-Record webhook with the private key; verify in-app with the embedded public key.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
